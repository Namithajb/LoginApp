package com.java.view;

public class PDFView /* extends AbstractPdfView */ {

	/*
	 * @Override protected void buildPdfDocument(Map<String, Object> model, Document
	 * document, PdfWriter writer, HttpServletRequest request, HttpServletResponse
	 * response) throws Exception { Invoice invoice = (Invoice)
	 * model.get("command");
	 * 
	 * Paragraph header = new Paragraph( new Chunk("Generate Pdf USing Spring Mvc",
	 * FontFactory.getFont(FontFactory.HELVETICA, 30))); Paragraph by = new
	 * Paragraph( new Chunk("Invoice Details " + invoice.getInvoice_id() + " " +
	 * invoice.getP_price(), FontFactory.getFont(FontFactory.HELVETICA, 20)));
	 * 
	 * document.add(header); document.add(by);
	 * 
	 * }
	 */

}
